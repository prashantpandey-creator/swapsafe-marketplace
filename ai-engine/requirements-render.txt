# ─── Core API ───────────────────────────────────────────────
fastapi==0.109.0
uvicorn==0.27.0
python-multipart==0.0.6
python-dotenv
aiofiles
aiohttp
requests
psutil

# ─── Image Processing ────────────────────────────────────────
pillow
numpy
onnxruntime          # rembg uses onnxruntime (NOT torch)
rembg[cpu]           # background removal — lightweight CPU variant
opencv-python-headless

# ─── AI / Search ─────────────────────────────────────────────
groq                 # price estimation (API calls only)
google-genai         # gemini flash analysis
duckduckgo-search>=5.0.0   # stock image search
googlesearch-python>=1.2.0
